services:
  webftpp:
    build: .
    container_name: webftpp
    ports:
      - "8001:80"
    environment:
      - APP_ENV=production
      - APP_KEY=base64:dDTxhEl8EqfCSB/JV3epjftAsPFHPM8uMTuBKI3eDKw=
      - APP_DEBUG=false
      - APP_URL=http://your-vps-ip/webftpp
      - DB_CONNECTION=mysql
      - DB_HOST=shared_mysql
      - DB_PORT=3306
      - DB_DATABASE=webftpp_db
      - DB_USERNAME=laravel_user
      - DB_PASSWORD=L@r@velP@ssw0rd123
      - CACHE_DRIVER=redis
      - SESSION_DRIVER=redis
      - REDIS_HOST=shared_redis
      - REDIS_PORT=6379
    volumes:
      - ./storage:/var/www/html/storage
      - ./bootstrap/cache:/var/www/html/bootstrap/cache
    restart: unless-stopped
    networks:
      - app_network

networks:
  app_network:
    external: true
